'''
A string is a sequence of characters enclosed in:

'single quotes'

"double quotes"

triple quotes(for multi-line)

📦 String Properties:
Strings are immutable (cannot be changed after creation).

You can access characters using index: s[0], s[-1], etc.

You can slice strings: s[0:4], s[:5], etc.


'''

name = "ramsingh"
print(name)

print(name[])