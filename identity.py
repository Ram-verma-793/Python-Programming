a = 5
b = 5
print(a is b) # returns true because a and b are the same
print(a is not b) # returns false because a and b are the same

# the identity operator is used to check if the variables are the same object or not